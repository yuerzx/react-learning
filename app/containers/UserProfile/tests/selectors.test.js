import {
  selectUserProfile,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectUserProfile();

describe('selectUserProfile', () => {

});
